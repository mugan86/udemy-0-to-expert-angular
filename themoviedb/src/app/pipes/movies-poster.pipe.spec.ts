import { MoviesPosterPipe } from './movies-poster.pipe';

describe('MoviesPosterPipe', () => {
  it('create an instance', () => {
    const pipe = new MoviesPosterPipe();
    expect(pipe).toBeTruthy();
  });
});
