import { NgDropZonesDirective } from './ng-drop-zones.directive';

describe('NgDropZonesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgDropZonesDirective();
    expect(directive).toBeTruthy();
  });
});
