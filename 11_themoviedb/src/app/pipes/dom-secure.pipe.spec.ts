import { DomSecurePipe } from './dom-secure.pipe';

describe('DomSecurePipe', () => {
  it('create an instance', () => {
    const pipe = new DomSecurePipe();
    expect(pipe).toBeTruthy();
  });
});
